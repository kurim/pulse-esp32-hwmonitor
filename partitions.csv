# Name,     Type, SubType,  Offset,   Size,     Flags
# 4 MB Flash. Zwei OTA-App-Slots (je 1.8125 MB) fuer OTA-Updates ueber das
# Webportal, angehoben von zuletzt 1.75 MB (davor 1.5 MB): das Image enthaelt
# Treiber fuer 5 Displaytypen gleichzeitig (ILI9341/9488/ST7796S/GC9A01/
# SSD1309) plus das eingebettete Bootlogo (bootlogo.c, ~24 KB als I8/indexed)
# und ueberschritt mit beidem zusammen die alten 1.75 MB ("program size ...
# greater than maximum allowed"). 1.8125 MB je Slot ist knapper bemessen als
# rechnerisch noetig waere, um Platz fuer die spiffs-Partition unten zu
# lassen (siehe dort) - Puffer zur zuletzt gemessenen Programmgroesse bleibt
# trotzdem ausreichend (~46 KB je Slot, mehr sobald die Weboberflaeche unten
# aus dem App-Image raus ist).
# otadata liegt bewusst bei 0xe000 (nicht 0xf000 wie im esp-idf-Original) -
# der Arduino-ESP32-Core/pioarduino-Flash-Vorgang schreibt bei jedem Upload
# automatisch boot_app0.bin (initialisiert otadata auf "boote App-Slot 0") an
# der fest einprogrammierten Adresse 0xe000, unabhaengig vom tatsaechlichen
# Inhalt dieser partitions.csv. Bei otadata=0xf000 ueberschrieb das die
# letzten 4 KB von nvs (Preferences-Daten!) und traf otadata nur zur Haelfte -
# das fuehrte bei jedem Flash zu Bootfehlern ("OTA app partition slot 1 is not
# bootable") und kaputten Preferences. nvs ist deshalb um 4 KB verkleinert.
# spiffs (trotz Namens mit LittleFS formatiert/gemountet, siehe
# board_build.filesystem in platformio.ini - "spiffs" ist hier nur der
# ESP-IDF-Partitions-Subtype-Name, unabhaengig vom tatsaechlichen
# Dateisystem) haelt die Webportal-Startseite (data/index.html, ~26 KB) statt
# sie wie bisher als C-String im App-Image mitzukompilieren - 128 KB Groesse
# mit deutlicher Reserve fuer spaetere Assets. Vor dem ersten Flashen bzw.
# nach Aenderungen an data/ einmalig zusaetzlich "pio run -t uploadfs"
# ausfuehren (siehe README), sonst bleibt die Partition leer.
# Insgesamt passen 2x 1.8125 MB + nvs/otadata/phy_init + 64 KB coredump +
# 128 KB spiffs in 4 MB, mit 64 KB unbelegtem Rest am Ende.
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
phy_init,   data, phy,      0x11000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x1D0000,
ota_1,      app,  ota_1,    0x1F0000, 0x1D0000,
coredump,   data, coredump,         , 0x10000,
spiffs,     data, spiffs,           , 0x20000,