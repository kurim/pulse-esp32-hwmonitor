# Name,     Type, SubType,  Offset,   Size,     Flags
# 4 MB Flash, zwei OTA-App-Slots (je 1.875 MB) fuer OTA-Updates ueber das
# Webportal. Auf 1.875 MB je Slot angehoben (vorher 1.75 MB, davor 1.5 MB):
# das Image enthaelt Treiber fuer 5 Displaytypen gleichzeitig (ILI9341/9488/
# ST7796S/GC9A01/SSD1309) plus das eingebettete Bootlogo (bootlogo.c, ~47 KB
# Bilddaten) und ueberschritt mit beidem zusammen die alten 1.75 MB
# ("program size ... greater than maximum allowed", ~41 KB Overflow).
# 2x 1.875 MB + nvs/otadata/phy_init passen weiterhin in 4 MB (rechnerisch
# bleiben ~128 KB frei), davon 64 KB fuer die coredump-Partition unten
# reserviert, ~64 KB bleiben ungenutzt - die vorherigen 1.75-MB-Slots hatten
# hier noch ~320 KB ungenutzten Flash am Ende uebrig, der jetzt zum groessten
# Teil fuer die zusaetzlichen 2x 128 KB je OTA-Slot verwendet wird.
# otadata liegt bewusst bei 0xe000 (nicht 0xf000 wie im esp-idf-Original) -
# der Arduino-ESP32-Core/pioarduino-Flash-Vorgang schreibt bei jedem Upload
# automatisch boot_app0.bin (initialisiert otadata auf "boote App-Slot 0") an
# der fest einprogrammierten Adresse 0xe000, unabhaengig vom tatsaechlichen
# Inhalt dieser partitions.csv. Bei otadata=0xf000 ueberschrieb das die
# letzten 4 KB von nvs (Preferences-Daten!) und traf otadata nur zur Haelfte -
# das fuehrte bei jedem Flash zu Bootfehlern ("OTA app partition slot 1 is not
# bootable") und kaputten Preferences. nvs ist deshalb um 4 KB verkleinert.
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
phy_init,   data, phy,      0x11000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x1E0000,
ota_1,      app,  ota_1,    0x200000, 0x1E0000,
coredump,   data, coredump,         , 0x10000,